# Passport & Audio Recognition Bot

Готовый к развёртыванию проект Telegram-бота с двумя бессерверными функциями
в Yandex Cloud: распознаёт данные паспорта с фото и извлекает номер телефона
и банк из голосового сообщения, объединяя всё в единый JSON-ответ.

## Состав репозитория

- `bot/` — исходный код Telegram-бота (Python, python-telegram-bot 13.15).
- `functions/passport/` — Cloud Function для OCR паспорта (Node.js).
- `functions/audio/` — Cloud Function для обработки голосовых сообщений (Node.js).
- `.env.example` — шаблон переменных окружения для локального запуска бота.

## Быстрый старт

### 1. Настройка Telegram-бота

```bash
cd bot
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt
cp ../env.example .env
```

Заполните `.env`, затем:

```bash
python main.py
```

### 2. Развёртывание функций

1. В каждой папке `functions/<name>` выполните:
   ```bash
   npm install
   zip -r function.zip .
   ```
2. Загрузите архив в Yandex Cloud Functions (Node.js 16), пропишите переменные
   окружения:
   - Passport: `YANDEX_VISION_API_KEY`, `YANDEX_GPT_API_KEY`, `YANDEX_FOLDER_ID`.
   - Audio: `YANDEX_SPEECHKIT_API_KEY`, `YANDEX_GPT_API_KEY`, `YANDEX_FOLDER_ID`.
3. Скопируйте HTTP URL функций и укажите их в `.env` бота.

## Основной сценарий

1. `/start` — бот показывает инструкцию (эмодзи, состояние `awaiting_passport`).
2. Пользователь отправляет фото паспорта → запрос в Passport Function →
   бот сохраняет JSON и переходит к `awaiting_audio`.
3. Пользователь отправляет голосовое сообщение →
   Audio Function → объединение данных → форматированный JSON в ответ →
   очистка сессии.

Дополнительно доступны `/status` и `/cancel`.

## Тестовые сценарии

- Позитив: полный happy-path, повторный `/start`, повторное распознавание.
- Негатив: неверные состояния (фото в ожидании аудио и наоборот),
  размытые фото, пустой voice, ошибки API, превышение размеров файлов.

## Продакшен-рекомендации

- Persist-сессии в БД, авторизация пользователей, rate limiting.
- Централизованное логирование и мониторинг Cloud Functions.
- Шифрование чувствительных данных и секретов.

Все ключи берутся только из переменных окружения, в репозитории нет хардкода.

## Загрузка в GitHub

📖 **Подробные инструкции:**
- [START_HERE.md](START_HERE.md) — начните отсюда (пошаговый план)
- [QUICK_START.md](QUICK_START.md) — быстрый чеклист
- [INSTALL_GIT.md](INSTALL_GIT.md) — пошаговая установка Git
- [GITHUB_TOKEN_GUIDE.md](GITHUB_TOKEN_GUIDE.md) — 🔑 **руководство по использованию токена**
- [GITHUB_SETUP.md](GITHUB_SETUP.md) — полная документация

**Краткая версия:**
1. Установите Git: https://git-scm.com/download/win
2. Выполните в папке проекта:
   ```bash
   git init
   git add .
   git commit -m "Initial commit: Telegram bot for passport and audio recognition"
   ```
3. Создайте репозиторий на GitHub и выполните:
   ```bash
   git remote add origin https://github.com/USERNAME/repo-name.git
   git branch -M main
   git push -u origin main
   ```


