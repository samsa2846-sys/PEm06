# 🔑 Подробное руководство: Использование токена GitHub

## Почему нужен токен вместо пароля?

С августа 2021 года GitHub **больше не принимает обычные пароли** при выполнении `git push` через HTTPS. Это сделано для безопасности. Вместо пароля нужно использовать **Personal Access Token (PAT)**.

## 📝 Пошаговая инструкция

### Шаг 1: Создание токена

1. Откройте: **https://github.com/settings/tokens**
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Заполните форму:
   - **Note**: `My Computer` или любое другое название (для вашего удобства)
   - **Expiration**: выберите срок действия:
     - `30 days` — для тестирования
     - `90 days` — для обычной работы
     - `No expiration` — токен никогда не истечет (менее безопасно)
   - **Select scopes**: поставьте галочку на **`repo`**
     - Это даст полный доступ к репозиториям (чтение, запись, удаление)
4. Прокрутите вниз и нажмите **"Generate token"** (зеленая кнопка)

### Шаг 2: Копирование токена

⚠️ **ВАЖНО:** Токен показывается **только один раз**!

1. Токен будет выглядеть примерно так: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`
2. **Сразу скопируйте его** (Ctrl+C)
3. Сохраните в безопасном месте (например, в менеджере паролей)
4. Если потеряете — придется создать новый

### Шаг 3: Использование токена при git push

Когда вы выполните команду:
```powershell
git push -u origin main
```

В терминале появится запрос:

```
Username for 'https://github.com': 
```

**Что вводить:**
- Введите ваш **GitHub username** (без символа @)
- Например, если ваш профиль: `https://github.com/ivanov`, то username: `ivanov`
- Нажмите Enter

Затем появится:
```
Password for 'https://ivanov@github.com': 
```

**Что вводить:**
- ⚠️ **НЕ вводите ваш обычный пароль GitHub!**
- Вставьте **скопированный токен** (Ctrl+V)
- При вводе токена символы **не будут отображаться** (это нормально для безопасности)
- Нажмите Enter

**Важно понимать:**
- В поле "Password" вы вводите **токен**, а не пароль
- Токен начинается с `ghp_` и содержит много символов
- При вводе ничего не видно — это нормально, просто вставьте токен и нажмите Enter

### Пример в терминале:

Вот как это выглядит в реальности:

```
PS C:\Cursor\test> git push -u origin main
Username for 'https://github.com': ivanov
Password for 'https://ivanov@github.com': 
```

**В этот момент:**
- Вы вставляете токен (Ctrl+V)
- На экране ничего не видно (курсор просто стоит)
- Это нормально! Просто нажмите Enter

После нажатия Enter:

```
Enumerating objects: 15, done.
Counting objects: 100% (15/15), done.
Delta compression using up to 8 threads
Compressing objects: 100% (12/12), done.
Writing objects: 100% (15/15), 5.23 KiB | 5.23 MiB/s, done.
Total 15 (delta 2), reused 0 (delta 0), pack-reused 0 (from 0)
remote: Resolving deltas: 100% (2/2), done.
To https://github.com/ivanov/passport-audio-bot.git
 * [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

✅ **Успех!** Код загружен на GitHub.

### Визуальная схема процесса:

```
1. Выполняете: git push -u origin main
   ↓
2. Запрос: Username for 'https://github.com': 
   → Вводите: ivanov (ваш username)
   → Нажимаете Enter
   ↓
3. Запрос: Password for 'https://ivanov@github.com': 
   → Вставляете токен: ghp_xxxxxxxxxxxx (Ctrl+V)
   → На экране ничего не видно (это нормально!)
   → Нажимаете Enter
   ↓
4. Git отправляет код на GitHub
   ↓
5. ✅ Готово! Код на GitHub
```

## 🔍 Частые вопросы

### Q: Почему при вводе токена ничего не видно?

**A:** Это нормально! Терминал скрывает ввод паролей/токенов для безопасности. Просто вставьте токен (Ctrl+V) и нажмите Enter.

### Q: Что делать, если токен не работает?

**A:** Проверьте:
1. Правильно ли скопирован токен (без лишних пробелов)
2. Не истек ли срок действия токена
3. Есть ли у токена права `repo`
4. Правильно ли введен username (без @ и без пробелов)

### Q: Можно ли использовать токен несколько раз?

**A:** Да! Токен можно использовать многократно, пока не истечет срок действия. Сохраните его в безопасном месте.

### Q: Как не вводить токен каждый раз?

**A:** Есть несколько способов:

**Способ 1: Git Credential Manager (рекомендуется)**
- Windows обычно устанавливает его вместе с Git
- При первом вводе токен сохранится
- В следующий раз вводить не нужно

**Способ 2: Сохранение в Git config (менее безопасно)**
```powershell
git config --global credential.helper store
```
⚠️ Токен сохранится в открытом виде в файле!

**Способ 3: Использование SSH вместо HTTPS**
- Настройте SSH ключи
- Не нужно вводить токен вообще

### Q: Где посмотреть мои токены?

**A:** https://github.com/settings/tokens
- Здесь видны все созданные токены
- Можно отозвать (revoke) старые токены
- Но сам токен после создания увидеть нельзя

### Q: Что делать, если забыл username?

**A:** 
1. Откройте https://github.com
2. Войдите в аккаунт
3. Ваш username будет в URL: `https://github.com/ВАШ_USERNAME`
4. Или в правом верхнем углу нажмите на аватар → там будет username

## ⚠️ Безопасность

1. **Не делитесь токеном** ни с кем
2. **Не коммитьте токен** в код (он уже в .gitignore)
3. **Отзовите токен**, если он скомпрометирован
4. **Используйте минимальные права** (только `repo` для этого проекта)
5. **Установите срок действия** токена

## 🎯 Альтернатива: GitHub Desktop

Если не хотите возиться с токенами:
- Используйте **GitHub Desktop** (https://desktop.github.com/)
- Он автоматически обрабатывает авторизацию
- Не нужно вводить токены вручную

---

**Нужна помощь?** Смотрите также:
- [START_HERE.md](START_HERE.md) — общая инструкция
- [INSTALL_GIT.md](INSTALL_GIT.md) — установка Git

