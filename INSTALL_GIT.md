# Пошаговая установка Git и загрузка проекта в GitHub

## Шаг 1: Установка Git

### Способ 1: Автоматическая установка (рекомендуется)

1. Откройте браузер и перейдите на: **https://git-scm.com/download/win**
2. Скачайте установщик (автоматически определит 64-bit или 32-bit)
3. Запустите скачанный файл (например, `Git-2.43.0-64-bit.exe`)
4. В установщике:
   - Нажимайте **"Next"** на всех экранах
   - Оставьте настройки по умолчанию
   - Выберите редактор (можно оставить "Nano" или выбрать "Notepad++")
   - Выберите "Git from the command line and also from 3rd-party software"
   - Нажмите **"Install"**
5. Дождитесь завершения установки
6. Нажмите **"Finish"**

### Способ 2: Через winget (если установлен)

Откройте PowerShell от имени администратора и выполните:
```powershell
winget install --id Git.Git -e --source winget
```

## Шаг 2: Проверка установки

1. **Закройте и откройте заново PowerShell** (важно для обновления PATH)
2. Выполните команду:
   ```powershell
   git --version
   ```
3. Должно появиться что-то вроде: `git version 2.43.0.windows.1`

## Шаг 3: Настройка Git (первый раз)

Выполните в PowerShell (замените на свои данные):

```powershell
git config --global user.name "Ваше Имя"
git config --global user.email "ваш.email@example.com"
```

**Пример:**
```powershell
git config --global user.name "Иван Иванов"
git config --global user.email "ivan@example.com"
```

## Шаг 4: Инициализация репозитория

Откройте PowerShell в папке проекта (`C:\Cursor\test`) и выполните:

```powershell
# Переход в папку проекта (если еще не там)
cd C:\Cursor\test

# Инициализация Git
git init

# Проверка статуса
git status
```

## Шаг 5: Добавление файлов и первый коммит

```powershell
# Добавление всех файлов
git add .

# Создание первого коммита
git commit -m "Initial commit: Telegram bot for passport and audio recognition"

# Проверка
git log
```

## Шаг 6: Создание репозитория на GitHub

1. Откройте **https://github.com** и войдите (или зарегистрируйтесь)
2. Нажмите кнопку **"+"** в правом верхнем углу
3. Выберите **"New repository"**
4. Заполните форму:
   - **Repository name**: `passport-audio-bot` (или другое название)
   - **Description**: `Telegram bot for passport and audio recognition using Yandex Cloud`
   - Выберите **Public** или **Private**
   - **НЕ** ставьте галочки на:
     - ❌ Add a README file
     - ❌ Add .gitignore
     - ❌ Choose a license
   - (У нас уже есть эти файлы!)
5. Нажмите **"Create repository"**

## Шаг 7: Подключение к GitHub

GitHub покажет инструкции. Выполните команды (замените `USERNAME` и `repo-name`):

```powershell
# Добавление удаленного репозитория
git remote add origin https://github.com/USERNAME/repo-name.git

# Переименование ветки в main
git branch -M main

# Отправка кода на GitHub
git push -u origin main
```

### Если запросит авторизацию:

GitHub больше не принимает пароли. Нужен **Personal Access Token**:

📖 **Подробное руководство:** [GITHUB_TOKEN_GUIDE.md](GITHUB_TOKEN_GUIDE.md)

**Краткая версия:**

1. Перейдите: **https://github.com/settings/tokens**
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Заполните:
   - **Note**: `My Computer` (любое название)
   - **Expiration**: выберите срок (например, 90 days)
   - **Select scopes**: поставьте галочку на **`repo`** (полный доступ)
4. Нажмите **"Generate token"**
5. **Скопируйте токен** (он показывается только один раз!)

**При выполнении `git push -u origin main`:**

- **Username**: введите ваш GitHub username (без @)
- **Password**: вставьте скопированный токен (не обычный пароль!)
- ⚠️ При вводе токена символы не отображаются — это нормально!

## Шаг 8: Проверка

Откройте ваш репозиторий на GitHub:
`https://github.com/USERNAME/repo-name`

Вы должны увидеть все файлы проекта!

## Альтернатива: GitHub Desktop (проще для новичков)

Если команды кажутся сложными:

1. Скачайте **GitHub Desktop**: https://desktop.github.com/
2. Установите и войдите в GitHub аккаунт
3. В GitHub Desktop:
   - **File** → **Add Local Repository**
   - Выберите папку `C:\Cursor\test`
   - Нажмите **"Publish repository"** (кнопка вверху)
   - Введите название и описание
   - Нажмите **"Publish repository"**

Готово! Код загружен на GitHub.

## Проблемы и решения

### "git: command not found"
- Перезапустите PowerShell/терминал
- Проверьте, что Git установлен: откройте "Git Bash" из меню Пуск

### "Permission denied" при push
- Проверьте правильность токена
- Убедитесь, что токен имеет права `repo`

### "Repository not found"
- Проверьте правильность URL репозитория
- Убедитесь, что репозиторий создан на GitHub

### Нужна помощь?
- Документация Git: https://git-scm.com/doc
- GitHub Help: https://docs.github.com


